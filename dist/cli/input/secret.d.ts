/**
 * Renders a masked one-line summary of a captured secret, surfacing only its
 * length so the raw value is never echoed to the UI.
 */
export declare function formatSecretInputSummary(value: string): string;
